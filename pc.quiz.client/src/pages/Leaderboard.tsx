import { Box, Typography } from "@mui/material";

const Leaderboard = () => {
    return (
        <Box>
            <Typography>
                Hello, this is leaderboard
            </Typography>
        </Box>
    );
}

export default Leaderboard;